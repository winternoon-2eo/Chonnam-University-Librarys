# [PRD] 전남대학교 도서관 전 분야 AI 스마트 큐레이터 (VibeLib)
> **문서 버전**: v5.0 (5대 방어 시스템 · 동시성 보호막 `SingleFlight` · `Two-Phase` 지연 로딩 · 새벽 4시 GitHub Actions 프리워밍 · 작가명 지능형 확장 전면 반영본)  
> **최종 수정일**: 2026-09-17  
> **상태**: Confirmed & Fully Implemented (실시간 라이브 연동, 무결성 20/20 앵커 통과, 회귀 테스트 5/5 통과)  
> **핵심 미션**: 전남대생이 어떤 책을 읽을지 고민하고 결정하는 데 드는 시간과 수고의 90%를 줄이고, 도서관 서가에서 즉시 대출할 수 있는 검증된 양서를 100% 신뢰할 수 있는 데이터로 제공한다.

---

## 1. 프로젝트 개요 & 비전

### 1.1. 서비스 정의
**VibeLib (전남대 AI 도서관 큐레이터)**는 전남대학교 도서관(lib.jnu.ac.kr)의 소장 도서를 대상으로, **사서 청구기호(DDC/KDC) 서가 탐색과 키워드 검색을 결합한 하이브리드 발굴**, **YES24 공식 실측 랭킹·판매지수·실구매자 후기 데이터 결합**, 그리고 **Gemini 3.5 Flash의 목차 심층 분석**을 통해 목적별 최적의 도서 5권을 즉각 큐레이션해 주는 지능형 도서 탐색 플랫폼입니다.

### 1.2. 해결하는 핵심 가치 (신뢰도 · 속도 · 안정성 혁신)
1. **캠퍼스별 분리 탐색 (광주 vs 여수)**: 여수캠퍼스와 광주캠퍼스(중앙도서관, 정보마루, 본관, 법학, 의학)의 소장 도서를 완벽히 분리하여 헛걸음 방지.
2. **사서 청구기호(DDC/KDC) 서가 하이브리드 발굴**: 도서명(제목)에 검색어가 없더라도, 해당 주제 서가(예: 프로그래밍 `005.1`, 글쓰기 `808`, 한국소설 `813` 등)에 꽂혀 있는 최신 명저를 함께 발굴.
3. **작가/인물명 지능형 확장**: 소설가/인물 검색 시 동음이의어 키워드 매칭(예: '한강' 검색 시 무관한 '코리아 유토피아: 제2의 한강의 기적' 추천) 결함을 차단하고, 실제 대표작과 문학 서가를 자동 연계.
4. **추천 신뢰성 극대화 (투명한 근거 제시)**:
   - 근거 없는 미사여구(가상의 분야 베스트, MIT 번역서 등)를 전면 배제하고, **YES24 공식 실측 랭킹 뱃지(`[베스트] {분야} {순위}`)**만 정직하게 노출.
   - **실제 구매자 후기 아코디언**을 통해 다른 독자들의 실제 평가 확인 가능.
   - **판매지수 산정 근거 툴팁** 제공.
   - **100% 무손실 원본 표지** 제공 (`object-contain`).
5. **목적별 해결 과제 브리핑**: 목차(TOC)를 정밀 분석하여 `[💡 이 책으로 해결 가능한 대학 과제/고민]` 2~3개를 명시.
6. **즉시 대출 가능성 보장 (Top 5 3:2 규칙)**: 추천 도서 5권 중 **최소 3권 이상 대출 가능(`AVAILABLE >= 3`)**, **대출중 도서는 최대 2권 이하(`CHECKED_OUT <= 2`)**로 제한하여 '발견 즉시 대출' 보장.
7. **초고속 2단계 지연 로딩 (Two-Phase Lazy Enrichment)**: 15권 후보군은 기본 메타데이터만 1초 만에 훑고, 최종 5권만 병렬로 상세 후기/랭킹을 보강하여 검색 속도를 4~5초 대로 80% 단축.
8. **다중 사용자 동시성 & 서버 포화 방지**:
   - `SingleFlight` 실시간 요청 병합으로 동일 쿼리 동시 유입 시 외부 API 부하 90% 차단.
   - 매일 새벽 4시 GitHub Actions 무료 자동 프리워밍으로 전남대 30대 핵심 학과/교양 키워드를 Supabase에 사전 적재(낮 시간 응답 속도 0.05초).

---

## 2. 타깃 사용자 페르소나

* **페르소나 1 (광주/여수 캠퍼스별 학부생)**: 본인이 소속된 캠퍼스 도서관에서 오늘 바로 빌려 읽을 수 있는 책을 찾는 학생.
* **페르소나 2 (과제/프로젝트 해결자)**: '논문 글쓰기', '바이브코딩', '데이터 분석' 등 당면한 과제와 레포트의 세부 고민을 해결해 줄 핵심 챕터가 수록된 실용서를 찾는 학생.
* **페르소나 3 (문학/교양 탐색자)**: 특정 작가(예: 한강)나 고전/인문학 분야에서 믿고 읽을 수 있는 공식 베스트셀러와 스테디셀러를 찾는 학생.
* **페르소나 4 (실패 없는 도서를 고르고 싶은 독자)**: 도서관의 오래된 책 더미 속에서 어떤 책이 검증된 인기 도서이고 최근 출간된 양서인지 판단하기 어려운 학생.

---

## 3. 기능 요구사항 명세 (Functional Requirements)

### 3.1. P0 (핵심 탐색 & 신뢰도 극대화 큐레이션)

#### FR-01: 캠퍼스 선택 및 소장처 정밀 분리 (Campus Scope Filter)
* **기능 설명**: 검색창 상단에서 소장 캠퍼스를 원클릭으로 선택.
* **선택 옵션**:
  1. `[🏛️ 광주캠퍼스]`: 중앙도서관[정보마루], 중앙도서관[본관], 법학도서관, 의학도서관 소장 도서만 필터링.
  2. `[🌊 여수캠퍼스]`: CNU OPAC `bk_2=jttjvyjttj` 파셋 및 `여수캠퍼스도서관` 소장 도서만 100% 매칭.
  3. `[🌐 전남대 전체]`: 전 캠퍼스 통합 검색.

#### FR-02: 자연어 질의 분석 & 청구기호(DDC/KDC) 서가 발굴 (Hybrid Harvesting)
* **기능 설명**:
  * 비정형 키워드, 축약어, 오탈자(예: '클롣', '바이브코딩', '코딩 공부')를 Gemini 3.5 Flash가 지능적으로 분석 및 표준 검색어로 정규화.
  * **소설가/인물 질의 특화**: 검색어가 인물/작가인 경우 대표작 도서명(예: ["한강", "소년이 온다", "작별하지 않는다", "채식주의자"])과 분류 청구기호(예: 한국소설 `813`)를 searchKeywords에 동시 탑재.
  * 질의에 해당하는 **DDC/KDC 청구기호 접두사(예: 글쓰기 ➔ `808`, 코딩 ➔ `005.1`, 문학 ➔ `813`) 자동 예측**.
  * CNU 도서관 청구기호 필드(`si=6&q={prefix}*`)를 통해 **동일 서가에 비치된 최신 도서를 병렬 탐색**.
  * 키워드 검색 결과와 청구기호 서가 결과를 중복 없이 병합(Hybrid Pool 구축).

#### FR-03: 15권 스마트 스크리닝 & 2단계 지연 로딩 (Two-Phase Lazy Pipeline)
* **기능 설명**:
  * CNU OPAC에서 50~100권을 고속 수집한 뒤, **최신순(2025~2026) 5권 + 대출가능/인기순 10권 = 총 15권**으로 압축 스크리닝.
  * **Phase 1 (기본 메타 인리치먼트)**: 15권 후보군에 대해 무거운 리뷰/랭킹 스크래핑을 생략하고 도서관 소장 정보와 서점 기본 메타만 1~1.5초 만에 고속 조회.
  * **Phase 2 (타깃 심층 인리치먼트)**: AI Curation(또는 Heuristic)이 엄선한 **최종 5권에 대해서만** YES24 실측 랭킹 뱃지, 구매자 후기, 도서관 세부 층별 서가를 병렬 호출(0.8초).
  * **성과**: 38초~100초 걸리던 콜드 검색 시간을 **4~5초 대로 80% 이상 단축**.

#### FR-04: YES24 공식 실측 데이터 연동 (Verified Book Metadata)
* **배경**: 알라딘 무료 OpenAPI 발급 중단에 따라 **YES24 공식 API(`yk_live_*`)로 전격 전환**.
* **데이터 연동 항목**:
  1. **공식 실측 랭킹 뱃지**: YES24 공식 실측 순위가 존재하는 도서에만 파란색 `[베스트] {분야} {랭킹}` 뱃지 노출 (실측치 없을 시 과감히 생략, 가상의 날조 뱃지 0% 차단).
  2. **회원들이 실제 쓴 후기 (Reviews Accordion)**: `<회원들이 실제 쓴 후기 (N개)>` 아코디언 제공. 클릭 시 별점, 마스킹된 작성자, 작성일, 구매자 리뷰 본문 인라인 표시 (후기 없을 시 '구매자 후기 없음' 정직 표기).
  3. **판매지수 산정 근거 툴팁**: 판매지수 옆 `[?]` 툴팁으로 "최근 4주간 판매량/주문액 가중치 반영 공식 지수" 안내.
  4. **100% 무손실 원본 표지**: `object-contain`을 적용하여 도서 표지 잘림 없이 온전한 원본 비율 노출.

#### FR-05: 목차(TOC) 기반 대학 과제/고민 해결 브리핑
* **기능 설명**:
  * YES24 상세 목차(Table of Contents)를 LLM이 심층 분석.
  * 도서 카드 전면에 `[💡 이 책으로 해결 가능한 대학 과제/고민]` 항목을 불렛포인트(2~3개)로 명시하여 책의 실질적 효용성 즉시 전달.

#### FR-06: Top 5 대출 가능 비율 엄격 보장 (Availability Ratio Enforcement)
* **기능 설명**:
  * 최종 Top 5 중 **대출 가능 도서 최소 3권 이상 (`AVAILABLE >= 3`)**, **대출 불가능(대출중) 도서 최대 2권 이하 (`CHECKED_OUT <= 2`)**을 강제하는 알고리즘 적용.
  * 대출중인 인기 도서가 상위를 차지하더라도 3위 이하의 대출중 도서는 즉시 대출 가능한 다음 순위 도서로 자동 교체.
  * 대출중 도서에는 반납 예정일을 주황색 뱃지로 명시.

---

### 3.2. P1 (안정성 & 동시성 방어 시스템)

#### FR-07: 5대 엔지니어링 방어 시스템 (5-Layer Defense Architecture)
1. **Layer 1 (VCS 형상 격리)**: Git MinGit 기반 `npm run snapshot`, `npm run rollback` 스냅샷 게이트로 실수로 인한 기능 퇴행 방지.
2. **Layer 2 (동시성 제어 & 요청 병합)**:
   - `SingleFlight`: 동일 쿼리(예: 30명이 동시에 '경영학' 검색) 유입 시 메모리 Map에서 요청을 병합하여 단 1회만 백엔드 실행 후 결과 공유.
   - 배치 큐잉(Batch Size 5, 딜레이 30ms)으로 도서관 및 서점 서버에 대한 429 차단 방지.
3. **Layer 3 (정적 무결성 앵커 검증)**: `scripts/check-integrity.mjs`를 통해 5개 핵심 파일 내 20대 핵심 앵커(랭킹 뱃지, 원본 표지, 3:2 대출 보장, 캠퍼스 분리 등)의 코드 변형을 자동 감시.
4. **Layer 4 (행동 계약 회귀 테스트)**: `tests/regression.test.mjs`를 통해 5개 핵심 비즈니스 계약의 정상 동작을 Node.js 테스트 러너로 상시 검증.
5. **Layer 5 (사전 배포 검증 게이트)**: `npm run verify` (`tsc --noEmit` + Layer 3 + Layer 4)를 모두 통과해야만 최종 빌드/배포를 승인.

#### FR-08: 새벽 4시 자동화 크롤러 & 캐시 프리워머 (Nightly Pre-warmer)
* **기능 설명**:
  * `scripts/prewarm-cache.mjs`: 전남대 30대 핵심 학과/교양 키워드(경영학, 파이썬, 토익, 회계, 간호학, 헌법, 한강, 시간관리 등)를 순회 수집.
  * `.github/workflows/prewarm-cache.yml`: 매일 새벽 04:00 KST (19:00 UTC) GitHub Actions 클라우드 러너에서 자동 실행.
  * 수집된 결과는 Supabase `books_cache`에 Upsert되어, 낮 시간 학생들의 검색 시 **응답 시간 0.05초(50ms), 외부 API 호출 0회**로 트래픽 폭주 완전 면역.

#### FR-09: 무중단 휴리스틱 자동 폴백 (Graceful Degradation)
* **기능 설명**:
  * Gemini API 호출 실패, 지연, 분당 쿼터(15 RPM) 초과 시에도 화면에 에러를 띄우지 않음.
  * YES24 실측 판매지수(5만점 이상 +45점, 2만점 이상 +30점) 및 공식 랭킹 뱃지(+40점) 기반의 고도화된 결정론적 휴리스틱 엔진(`fallbackHeuristicCuration`)으로 0.1초 만에 자동 전환되어 안정적인 결과 반환.

---

### 3.3. P2 (알림 & 미소장 도서 처리 및 수익화)

* **FR-10: 스마트 반납 웹 푸시 알림 (Web Push / PWA)**:
  * 대출 중인 도서에 대해 [반납 알림 받기] 등록 시, 별도 비용 0원인 웹 푸시로 스마트폰 상단 팝업 알림.
* **FR-11: 미소장 도서 투 트랙 처리**:
  * 전남대 도서관에 없는 책일 경우:
    1. **[도서관에 사달라고 하기]**: 전남대 희망도서 신청 페이지 즉시 연결.
    2. **[내일 아침 배송 / 전자책 보기]**: 온라인 서점 제휴 구매 링크 제공.
* **FR-12: 디스플레이 광고 배너 & 서점 제휴 마케팅**:
  * 검색 결과 중간/하단 애드센스 반응형 배너.
  * 대출 대기 2인 이상 또는 미소장 도서에 한해 전자책/배송 구매 제휴 링크 노출 (수수료 3% 확보).
  * 승인 통과를 위한 큐레이션 칼럼 매거진(`/articles`) 정적 탑재.

---

## 4. 지속가능성 & 운영 비용 최적화 (Sustainability)

1. **복합 키 기반 2단계 캐싱 (`${query}_${intent}_${campus}`)**:
   - 캠퍼스, 검색 목적, 정규화된 질의를 조합한 복합 키로 캐싱.
   - 캐시 적중 시 **응답 속도 0.05초, API 비용 0원**.
   - 24시간 LRU 캐시 및 Supabase 저장소 활용으로 월 AI 운영비를 커피 1잔 값 수준으로 유지.
2. **Two-Phase 호출 감소율**:
   - 기존: 15권 × 4회 네트워크 요청 = 60회
   - 개편: 15권 기본(15회) + Top 5 심층(5회) = 20회 (**호출 횟수 66% 절감**).

---

## 5. 핵심 성공 지표 (KPIs)

1. **도서 결정 시간**: 검색부터 대출 결정까지 **1분 이내 완료** (기존 핑퐁 검색 대비 90% 단축).
2. **콜드 검색 응답 속도**: 15권 스크리닝 및 큐레이션 완료까지 **5초 이내 달성** (기존 38초~100초 대비 80% 개선).
3. **추천 신뢰도 만족도**: 실측 랭킹 뱃지, 구매자 후기, 목차 기반 과제 해결 코멘트를 통한 추천 신뢰도 95% 이상 확보.
4. **즉시 대출 전환율**: Top 5 도서 중 1권 이상을 당일 도서관에서 대출하는 사용자 비율 40% 이상 달성.
5. **동시성 장애율 (429 / Timeout)**: 30명 동시 검색 유입 시 장애율 0% (SingleFlight 및 새벽 프리워밍 적용).
